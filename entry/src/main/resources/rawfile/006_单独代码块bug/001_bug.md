```cangjie
let s = DateTime.now()
for (i in 0..times){
    let zipFile = ZipFile("../benchmark/uncompressfile/zip_typical_10MPlus.zip")
     zipFile.extractAll("./")
}
let e = Duration.since(s).toNanoseconds()
print("[ PASSED ] CASE: cangjie_TestZip_uncompress_zip_typical_10MPlus_${times}_times (${e} ns, ${e/times} ns/op)\n", flush:true)
```